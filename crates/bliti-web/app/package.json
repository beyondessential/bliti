{
	"name": "bliti-web-app",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"prebuild": "just build-wasm",
		"predev": "just build-wasm",
		"pretest": "just build-wasm",
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"test": "playwright test"
	},
	"dependencies": {
		"react": "^19.2.0",
		"react-dom": "^19.2.0"
	},
	"devDependencies": {
		"@playwright/test": "^1.56.0",
		"@vitejs/plugin-react": "^5.1.0",
		"vite": "^7.1.0",
		"vite-plugin-pwa": "^1.0.0"
	}
}
